services:
  departarr:
    image: ghcr.io/basedqani/departarr:latest
    # build: .   # uncomment to build from source
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
